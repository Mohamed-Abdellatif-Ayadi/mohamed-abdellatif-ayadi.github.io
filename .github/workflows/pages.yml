
name: Deploy frontend to GitHub Pages
on:
  push:
    branches: [ main ]
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: "pages"
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: client/package-lock.json
      - name: Install client dependencies
        run: |
          cd client
          npm ci
      - name: Build frontend
        run: |
          cd client
          npm run build
      - name: Copy all static assets
        run: |
          # Copy client/public contents to dist (Vite should handle this but ensure it's done)
          if [ -d "client/public" ]; then
            cp -r client/public/* client/dist/ 2>/dev/null || true
          fi
          # Copy root public contents to dist  
          if [ -d "public" ]; then
            cp -r public/* client/dist/ 2>/dev/null || true
          fi
      - name: SPA 404 fallback
        run: cp "client/dist/index.html" "client/dist/404.html"
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: client/dist
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
